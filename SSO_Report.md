# Lazy loaded Scripts
* Lazy Loaded script found in file `theme.alternate.liquid` at line `674` in folder `layout`:
```html
      <script defer="defer" type="noscript-s" data-src="https://www.dwin1.com/20973.js"></script>

```
* Lazy Loaded script found in file `theme.liquid` at line `695` in folder `layout`:
```html
      <script defer="defer" type="noscript-s" data-src="https://www.dwin1.com/20973.js"></script>

```
* Lazy Loaded script found in file `__opt-theme-headers.liquid` at line `36` in folder `snippets`:
```html
<script type="noscript-s">
</script>
```
* Lazy Loaded script found in file `__opt-theme-js.liquid` at line `10` in folder `snippets`:
```html
<script type="noscript-s">
  if(videos){
    videos.forEach(function (element) {  
      element.setAttribute('src', element.getAttribute('data-src'));
      element.removeAttribute('data-src');
      element.closest('video').load();
    });
    playPauseVideo();
  }
</script>

```
* Lazy Loaded script found in file `__opt-theme-observer.liquid` at line `304` in folder `snippets`:
```html
<script type="noscript-s"> 

  setTimeout(() => {
    optObserver.disconnect();
    window.yett.unblock();
    ... 
    [Script content trimmed for brevity]
</script>
```
# Lazy loaded Styles
* Lazy loaded css file, found in file `theme.alternate.liquid` at line `311` in folder `layout`:
```html
<link data-href="{{ 'base.css' | asset_url }}" rel="stylesheet" type="text/css" media="all">
```
* Lazy loaded css file, found in file `theme.alternate.liquid` at line `314` in folder `layout`:
```html
<link data-href="{{ 'base.css' | asset_url }}" rel="stylesheet" type="text/css" media="all">
```
* Lazy loaded css file, found in file `theme.alternate.liquid` at line `317` in folder `layout`:
```html
<link data-href="{{ 'base.css' | asset_url }}" rel="stylesheet" type="text/css" media="all">
```
* Lazy loaded css file, found in file `theme.alternate.liquid` at line `324` in folder `layout`:
```html
<link data-href="{{ 'base.css' | asset_url }}" rel="stylesheet" type="text/css" media="all">
```
* Lazy loaded css file, found in file `theme.liquid` at line `328` in folder `layout`:
```html
<link data-href="{{ 'base.css' | asset_url }}" rel="stylesheet" type="text/css" media="all">
```
* Lazy loaded css file, found in file `theme.liquid` at line `331` in folder `layout`:
```html
<link data-href="{{ 'base.css' | asset_url }}" rel="stylesheet" type="text/css" media="all">
```
* Lazy loaded css file, found in file `theme.liquid` at line `334` in folder `layout`:
```html
<link data-href="{{ 'base.css' | asset_url }}" rel="stylesheet" type="text/css" media="all">
```
* Lazy loaded css file, found in file `grid-collection-custom.liquid` at line `6` in folder `sections`:
```html
<link data-href="{{ 'template-collection.css' | asset_url }}" rel="stylesheet" type="text/css" media="all">
```
* Lazy loaded css file, found in file `grid-collection-custom.liquid` at line `7` in folder `sections`:
```html
<link data-href="{{ 'component-loading-overlay.css' | asset_url }}" rel="stylesheet" type="text/css" media="all">
```
* Lazy loaded css file, found in file `grid-collection-custom.liquid` at line `8` in folder `sections`:
```html
{% comment %} <link data-href="{{ 'component-card.css' | asset_url }}" rel="stylesheet" type="text/css" media="all"> {% endcomment %}
```
* Lazy loaded css file, found in file `grid-collection-custom.liquid` at line `9` in folder `sections`:
```html
<link data-href="{{ 'component-price.css' | asset_url }}" rel="stylesheet" type="text/css" media="all">
```
* Lazy loaded css file, found in file `grid-collection-custom.liquid` at line `109` in folder `sections`:
```html
<link data-href="{{ 'component-facets.css' | asset_url }}" rel="stylesheet" type="text/css" media="all">
```
* Lazy loaded css file, found in file `image-banner.liquid` at line `2` in folder `sections`:
```html
{% comment %}<link data-href="{{ 'section-image-banner.css' | asset_url }}" rel="stylesheet" type="text/css" media="all">{% endcomment %}
```
* Lazy loaded css file, found in file `main-collection-product-grid.liquid` at line `6` in folder `sections`:
```html
<link data-href="{{ 'template-collection.css' | asset_url }}" rel="stylesheet" type="text/css" media="all">
```
* Lazy loaded css file, found in file `main-collection-product-grid.liquid` at line `7` in folder `sections`:
```html
<link data-href="{{ 'component-loading-overlay.css' | asset_url }}" rel="stylesheet" type="text/css" media="all">
```
* Lazy loaded css file, found in file `main-collection-product-grid.liquid` at line `8` in folder `sections`:
```html
<link data-href="{{ 'component-card.css' | asset_url }}" rel="stylesheet" type="text/css" media="all">
```
* Lazy loaded css file, found in file `main-collection-product-grid.liquid` at line `9` in folder `sections`:
```html
<link data-href="{{ 'component-price.css' | asset_url }}" rel="stylesheet" type="text/css" media="all">
```
* Lazy loaded css file, found in file `main-collection-product-grid.liquid` at line `85` in folder `sections`:
```html
<link data-href="{{ 'component-facets.css' | asset_url }}" rel="stylesheet" type="text/css" media="all">
```
* Lazy loaded css file, found in file `main-product.liquid` at line `1` in folder `sections`:
```html
{%- comment -%}<link data-href="{{ 'section-main-product.css' | asset_url }}" rel="stylesheet" type="text/css" media="all">{%- endcomment -%}
```
* Lazy loaded css file, found in file `main-product.liquid` at line `3` in folder `sections`:
```html
<link data-href="{{ 'component-accordion.css' | asset_url }}" rel="stylesheet" type="text/css" media="all">
```
* Lazy loaded css file, found in file `main-product.liquid` at line `4` in folder `sections`:
```html
<link data-href="{{ 'component-price.css' | asset_url }}" rel="stylesheet" type="text/css" media="all">
```
* Lazy loaded css file, found in file `main-product.liquid` at line `5` in folder `sections`:
```html
<link data-href="{{ 'component-rte.css' | asset_url }}" rel="stylesheet" type="text/css" media="all">
```
* Lazy loaded css file, found in file `main-product.liquid` at line `6` in folder `sections`:
```html
<link data-href="{{ 'component-slider.css' | asset_url }}" rel="stylesheet" type="text/css" media="all">
```
* Lazy loaded css file, found in file `main-product.liquid` at line `7` in folder `sections`:
```html
<link data-href="{{ 'component-rating.css' | asset_url }}" rel="stylesheet" type="text/css" media="all">
```
* Lazy loaded css file, found in file `main-product.liquid` at line `8` in folder `sections`:
```html
<link data-href="{{ 'component-loading-overlay.css' | asset_url }}" rel="stylesheet" type="text/css" media="all">
```
* Lazy loaded css file, found in file `main-product.liquid` at line `9` in folder `sections`:
```html
<link data-href="{{ 'component-deferred-media.css' | asset_url }}" rel="stylesheet" type="text/css" media="all">
```
* Lazy loaded css file, found in file `trynow-collection-product-grid.liquid` at line `6` in folder `sections`:
```html
<link data-href="{{ 'template-collection.css' | asset_url }}" rel="stylesheet" type="text/css" media="all">
```
* Lazy loaded css file, found in file `trynow-collection-product-grid.liquid` at line `7` in folder `sections`:
```html
<link data-href="{{ 'component-loading-overlay.css' | asset_url }}" rel="stylesheet" type="text/css" media="all">
```
* Lazy loaded css file, found in file `trynow-collection-product-grid.liquid` at line `8` in folder `sections`:
```html
<link data-href="{{ 'component-card.css' | asset_url }}" rel="stylesheet" type="text/css" media="all">
```
* Lazy loaded css file, found in file `trynow-collection-product-grid.liquid` at line `9` in folder `sections`:
```html
<link data-href="{{ 'component-price.css' | asset_url }}" rel="stylesheet" type="text/css" media="all">
```
* Lazy loaded css file, found in file `trynow-collection-product-grid.liquid` at line `44` in folder `sections`:
```html
<link data-href="{{ 'component-facets.css' | asset_url }}" rel="stylesheet" type="text/css" media="all">
```
# Lazy loaded `<img>` tags with class `opt_lazy` 
No images were found.
# `<script>` tags with class `restructure`
* `<script>` tag with class `restructure`, found in file `ab-header.liquid` at line `747` in folder `sections`:
```html
    <script class="restructure">
      document.querySelector('#Details-menu-drawer-container').addEventListener('click', function(){
        if(!jsLoad){
          this.classList.toggle('menu-opening');
        }
      }); 
    </script>

```

* `<script>` tag with class `restructure`, found in file `ab-header.liquid` at line `766` in folder `sections`:
```html
    <script class="restructure">
  
      {% comment %} Open Cart Drawer  {% endcomment %}
      document.querySelector('#Details-cart-drawer-container').addEventListener('click', function(){
        if(!jsWNWLoad){
          this.classList.toggle('menu-opening');
        }
      });
  
      {% comment %} Close Cart Drawer  {% endcomment %}
      document.querySelector('.drawer__close').addEventListener('click', function() {
        console.log("Clicked", this.closest('#Details-cart-drawer-container'))
        if (!jsWNWLoad) {
          document.querySelector('#Details-cart-drawer-container').removeAttribute('open');
        }
      });
    </script>

```

* `<script>` tag with class `restructure`, found in file `header.liquid` at line `660` in folder `sections`:
```html
  <script class="restructure">
    document.querySelector('#Details-menu-drawer-container').addEventListener('click', function(){
      if(!jsLoad){
        this.classList.toggle('menu-opening');
      }
    }); 
  </script>

```

* `<script>` tag with class `restructure`, found in file `header.liquid` at line `679` in folder `sections`:
```html
  <script class="restructure">

    {% comment %} Open Cart Drawer  {% endcomment %}
    document.querySelector('#Details-cart-drawer-container').addEventListener('click', function(){
      if(!jsWNWLoad){
        this.classList.toggle('menu-opening');
      }
    });

    {% comment %} Close Cart Drawer  {% endcomment %}
    document.querySelector('.drawer__close').addEventListener('click', function() {
      console.log("Clicked", this.closest('#Details-cart-drawer-container'))
      if (!jsWNWLoad) {
        document.querySelector('#Details-cart-drawer-container').removeAttribute('open');
      }
    });
  </script>

```

